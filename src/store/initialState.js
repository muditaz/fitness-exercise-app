import { defaultBodyPartSelected } from "../constants/constants";

const initialState = {
    bodyPartSelected: defaultBodyPartSelected,
    bodyParts: [],
    exercises: []
};

export default initialState;